apiVersion: v1
kind: Service
metadata:
  name: distributed-daftar
  namespace: distributed-daftar
spec:
  type: NodePort
  selector:
    app: distributed-daftar
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30080
